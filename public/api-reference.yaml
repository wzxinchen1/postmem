# PostMem API Reference (for LLM)
# 自动生成的精简文档，供大模型理解 API 结构

---

## Knowledge Base

### POST /api/kb/create

**Types:** CreateKBRequest (请求), KnowledgeBaseInfo (响应)

Request Body:
  type: CreateKBRequest
  - `name`: string (required)
  - `description`: string

Response Body:
  200: 成功响应 (KnowledgeBaseInfo)
    { success: boolean, data: KnowledgeBaseInfo }
  201: 创建成功 (KnowledgeBaseInfo)
    { success: boolean, data: KnowledgeBaseInfo }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

### POST /api/kb/delete

**Types:** DeleteRequest (请求), void (响应)

Request Body:
  type: DeleteRequest
  - `id`: number (required)

Response Body:
  200: 成功响应 (void)
    { success: boolean, data: void }
  201: 创建成功 (void)
    { success: boolean, data: void }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

### POST /api/kb/list

**Types:** ListRequest (请求), ListItem[] (响应)

Request Body:
  type: ListRequest
  - `kbId`: number (required)
  - `page`: number
  - `limit`: number

Response Body:
  200: 成功响应 (ListItem[])
    { success: boolean, data: ListItem[] }
  201: 创建成功 (ListItem[])
    { success: boolean, data: ListItem[] }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

### POST /api/kb/search

**Types:** SearchRequest (请求), SearchResult[] (响应)

Request Body:
  type: SearchRequest
  - `kbId`: number (required)
  - `query`: string (required)
  - `top_k`: number
  - `context_window`: number

Response Body:
  200: 成功响应 (SearchResult[])
    { success: boolean, data: SearchResult[] }
  201: 创建成功 (SearchResult[])
    { success: boolean, data: SearchResult[] }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

### POST /api/kb/stats

**Types:** StatsRequest (请求), Stats (响应)

Request Body:
  type: StatsRequest
  - `kbId`: number

Response Body:
  200: 成功响应 (Stats)
    { success: boolean, data: Stats }
  201: 创建成功 (Stats)
    { success: boolean, data: Stats }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

---

## Models

### GET /api/models/{id}
单个模型 API

**Types:** Model (响应)

Path Parameters:
  - `id`: integer (required) - 模型ID

Response Body:
  200: 成功响应 (Model)
    { success: boolean, data: Model }
  404: 资源不存在 (ErrorResponse)

### PUT /api/models/{id}
单个模型 API

**Types:** UpdateModelRequest (请求), Model (响应)

Path Parameters:
  - `id`: integer (required) - 模型ID

Request Body:
  type: UpdateModelRequest
  - `name`: string
  - `displayName`: string
  - `modelType`: ModelType
  - `config`: unknown
  - `isActive`: boolean
  - `isDefault`: boolean

Response Body:
  200: 成功响应 (Model)
    { success: boolean, data: Model }
  404: 资源不存在 (ErrorResponse)

### DELETE /api/models/{id}
单个模型 API

**Types:** Model (响应)

Path Parameters:
  - `id`: integer (required) - 模型ID

Response Body:
  200: 成功响应 (Model)
    { success: boolean, data: Model }
  404: 资源不存在 (ErrorResponse)

### GET /api/models/default
获取默认模型 API

**Types:** Model (响应)

Query Parameters:
  - `modelType`: string [chat|embedding] - 模型类型

Response Body:
  200: 成功响应 (Model)
    { success: boolean, data: Model }

### GET /api/models/index
模型列表和创建 API

**Types:** Model[] (响应)

Query Parameters:
  - `includeInactive`: boolean (默认: false) - 是否包含已禁用的项
  - `providerId`: integer - 按提供商ID筛选

Response Body:
  200: 成功响应 (Model[])
    { success: boolean, data: Model[] }

### POST /api/models/index
模型列表和创建 API

**Types:** CreateModelRequest (请求), Model (响应)

Query Parameters:
  - `includeInactive`: boolean (默认: false) - 是否包含已禁用的项
  - `providerId`: integer - 按提供商ID筛选

Request Body:
  type: CreateModelRequest
  - `providerId`: number (required)
  - `name`: string (required)
  - `displayName`: string
  - `modelType`: ModelType (required)
  - `config`: unknown
  - `isActive`: boolean
  - `isDefault`: boolean

Response Body:
  200: 成功响应 (Model)
    { success: boolean, data: Model }
  201: 创建成功 (Model)
    { success: boolean, data: Model }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

---

## Providers

### GET /api/providers/{id}
单个提供商 API

**Types:** Provider (响应)

Path Parameters:
  - `id`: integer (required) - 提供商ID

Response Body:
  200: 成功响应 (Provider)
    { success: boolean, data: Provider }
  404: 资源不存在 (ErrorResponse)

### PUT /api/providers/{id}
单个提供商 API

**Types:** UpdateProviderRequest (请求), Provider (响应)

Path Parameters:
  - `id`: integer (required) - 提供商ID

Request Body:
  type: UpdateProviderRequest
  - `name`: string
  - `vendorId`: number
  - `apiKey`: string
  - `baseUrl`: string
  - `config`: unknown
  - `isActive`: boolean

Response Body:
  200: 成功响应 (Provider)
    { success: boolean, data: Provider }
  404: 资源不存在 (ErrorResponse)

### DELETE /api/providers/{id}
单个提供商 API

**Types:** Provider (响应)

Path Parameters:
  - `id`: integer (required) - 提供商ID

Response Body:
  200: 成功响应 (Provider)
    { success: boolean, data: Provider }
  404: 资源不存在 (ErrorResponse)

### GET /api/providers/index
提供商列表和创建 API

**Types:** Provider[] (响应)

Query Parameters:
  - `includeInactive`: boolean (默认: false) - 是否包含已禁用的项

Response Body:
  200: 成功响应 (Provider[])
    { success: boolean, data: Provider[] }

### POST /api/providers/index
提供商列表和创建 API

**Types:** CreateProviderRequest (请求), Provider (响应)

Query Parameters:
  - `includeInactive`: boolean (默认: false) - 是否包含已禁用的项

Request Body:
  type: CreateProviderRequest
  - `name`: string (required)
  - `vendorId`: number (required)
  - `apiKey`: string
  - `baseUrl`: string (required)
  - `config`: unknown
  - `isActive`: boolean

Response Body:
  200: 成功响应 (Provider)
    { success: boolean, data: Provider }
  201: 创建成功 (Provider)
    { success: boolean, data: Provider }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

### POST /api/providers/models

**Types:** Model[] (响应)

Request Body:
  - `vendorId`: string (required)
  - `apiKey`: string (required)
  - `baseUrl`: string (required)

Response Body:
  200: 成功响应 (Model[])
    { success: boolean, data: Model[] }
  201: 创建成功 (Model[])
    { success: boolean, data: Model[] }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

### POST /api/providers/validate

**Types:** boolean (响应)

Request Body:
  - `vendorId`: string (required)
  - `apiKey`: string (required)
  - `baseUrl`: string (required)

Response Body:
  200: 成功响应 (boolean)
    { success: boolean, data: boolean }
  201: 创建成功 (boolean)
    { success: boolean, data: boolean }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

---

## Sessions

### GET /api/sessions/{id}
会话详情和删除 API

**Types:** Session (响应)

Path Parameters:
  - `id`: integer (required) - 会话ID

Response Body:
  200: 成功响应 (Session)
    { success: boolean, data: Session }
  404: 资源不存在 (ErrorResponse)

### DELETE /api/sessions/{id}
会话详情和删除 API

**Types:** Session (响应)

Path Parameters:
  - `id`: integer (required) - 会话ID

Response Body:
  200: 成功响应 (Session)
    { success: boolean, data: Session }
  404: 资源不存在 (ErrorResponse)

### GET /api/sessions/index
会话列表 API

**Types:** Session[] (响应)

Query Parameters:
  - `kbId`: integer (required) - 按知识库筛选
  - `modelType`: string [chat|embedding] - 模型类型
  - `status`: string [pending|completed|failed] - 按状态筛选
  - `page`: integer (默认: 1) - 页码
  - `limit`: integer (默认: 20) - 每页数量

Response Body:
  200: 成功响应 (Session[])
    { success: boolean, data: Session[] }

### GET /api/sessions/stats
会话统计 API

**Types:** Stats (响应)

Response Body:
  200: 成功响应 (Stats)
    { success: boolean, data: Stats }

---

## Settings

### GET /api/settings/index
应用设置 API

**Types:** AppSettings (响应)

Response Body:
  200: 成功响应 (AppSettings)
    { success: boolean, data: AppSettings }

### PUT /api/settings/index
应用设置 API

**Types:** AppSettings (响应)

Response Body:
  200: 成功响应 (AppSettings)
    { success: boolean, data: AppSettings }

---

## Vendors

### GET /api/vendors/{id}
单个厂商 API

**Types:** Vendor (响应)

Path Parameters:
  - `id`: integer (required) - 厂商ID

Response Body:
  200: 成功响应 (Vendor)
    { success: boolean, data: Vendor }
  404: 资源不存在 (ErrorResponse)

### PUT /api/vendors/{id}
单个厂商 API

**Types:** UpdateVendorRequest (请求), Vendor (响应)

Path Parameters:
  - `id`: integer (required) - 厂商ID

Request Body:
  type: UpdateVendorRequest
  - `name`: string
  - `chatModelClass`: string
  - `embeddingModelClass`: string
  - `factoryCode`: string
  - `isActive`: boolean

Response Body:
  200: 成功响应 (Vendor)
    { success: boolean, data: Vendor }
  404: 资源不存在 (ErrorResponse)

### DELETE /api/vendors/{id}
单个厂商 API

**Types:** Vendor (响应)

Path Parameters:
  - `id`: integer (required) - 厂商ID

Response Body:
  200: 成功响应 (Vendor)
    { success: boolean, data: Vendor }
  404: 资源不存在 (ErrorResponse)

### GET /api/vendors/index
厂商列表和创建 API

**Types:** Vendor[] (响应)

Query Parameters:
  - `includeInactive`: boolean (默认: false) - 是否包含已禁用的项

Response Body:
  200: 成功响应 (Vendor[])
    { success: boolean, data: Vendor[] }

### POST /api/vendors/index
厂商列表和创建 API

**Types:** CreateVendorRequest (请求), Vendor (响应)

Query Parameters:
  - `includeInactive`: boolean (默认: false) - 是否包含已禁用的项

Request Body:
  type: CreateVendorRequest
  - `name`: string (required)
  - `chatModelClass`: string
  - `embeddingModelClass`: string
  - `factoryCode`: string
  - `isActive`: boolean

Response Body:
  200: 成功响应 (Vendor)
    { success: boolean, data: Vendor }
  201: 创建成功 (Vendor)
    { success: boolean, data: Vendor }
  400: 请求参数错误 (ErrorResponse)
  409: 冲突（重复） (ErrorResponse)

---

## Data Types

### MemoryMetadata
Required:
key

- `cutModel`?: string
- `messageId`?: string
- `role`?: string
- `key`*: string]: unknown

### SearchResult
Required:
id, title, content, score, topicId, metadata, source, prev, next

- `id`*: number
- `title`*: string
- `content`*: string
- `score`*: number
- `topicId`*: number | null
- `metadata`*: MemoryMetadata
- `source`*: SearchSource
- `context`?: {
- `prev`*: string[]
- `next`*: string[]

### ListItem
Required:
id, title, content, topicId, metadata, createdAt

- `id`*: number
- `title`*: string
- `content`*: string
- `topicId`*: number | null
- `metadata`*: MemoryMetadata
- `createdAt`*: Date

### Stats
Required:
total

- `kbId`?: number
- `kbName`?: string
- `total`*: number
- `lastUpdated`?: Date

### SearchRequest
Required:
kbId, query

- `kbId`*: number
- `query`*: string
- `top_k`?: number
- `context_window`?: number

### ListRequest
Required:
kbId

- `kbId`*: number
- `page`?: number
- `limit`?: number

### DeleteRequest
Required:
id

- `id`*: number

### StatsRequest

- `kbId`?: number

### Vendor
Required:
id, name, url, isActive, createdAt, updatedAt

- `id`*: number
- `name`*: string
- `url`*: string
- `chatModelClass`?: string | null
- `embeddingModelClass`?: string | null
- `factoryCode`?: string | null
- `isActive`*: boolean
- `createdAt`*: Date
- `updatedAt`*: Date

### Provider
Required:
id, name, vendorId, baseUrl, config, isActive, createdAt, updatedAt

- `id`*: number
- `name`*: string
- `vendorId`*: number
- `vendor`?: Vendor
- `apiKey`?: string
- `baseUrl`*: string
- `config`*: unknown
- `isActive`*: boolean
- `createdAt`*: Date
- `updatedAt`*: Date

### Model
Required:
id, providerId, name, modelType, config, isActive, isDefault, createdAt, updatedAt

- `id`*: number
- `providerId`*: number
- `name`*: string
- `displayName`?: string
- `modelType`*: ModelType
- `config`*: unknown
- `isActive`*: boolean
- `isDefault`*: boolean
- `createdAt`*: Date
- `updatedAt`*: Date

### CreateProviderRequest
Required:
name, vendorId, baseUrl

- `name`*: string
- `vendorId`*: number
- `apiKey`?: string
- `baseUrl`*: string
- `config`?: unknown
- `isActive`?: boolean

### UpdateProviderRequest

- `name`?: string
- `vendorId`?: number
- `apiKey`?: string
- `baseUrl`?: string
- `config`?: unknown
- `isActive`?: boolean

### CreateVendorRequest
Required:
name

- `name`*: string
- `chatModelClass`?: string
- `embeddingModelClass`?: string
- `factoryCode`?: string
- `isActive`?: boolean

### UpdateVendorRequest

- `name`?: string
- `chatModelClass`?: string
- `embeddingModelClass`?: string
- `factoryCode`?: string
- `isActive`?: boolean

### CreateModelRequest
Required:
providerId, name, modelType

- `providerId`*: number
- `name`*: string
- `displayName`?: string
- `modelType`*: ModelType
- `config`?: unknown
- `isActive`?: boolean
- `isDefault`?: boolean

### UpdateModelRequest

- `name`?: string
- `displayName`?: string
- `modelType`?: ModelType
- `config`?: unknown
- `isActive`?: boolean
- `isDefault`?: boolean

### AppSettings
Required:
maxContentLength, defaultTopK, defaultContextWindow, defaultPageSize

- `maxContentLength`*: number
- `defaultTopK`*: number
- `defaultContextWindow`*: number
- `defaultPageSize`*: number

### CreateKBRequest
Required:
name

- `name`*: string
- `description`?: string

### KnowledgeBaseInfo
Required:
id, name, createdAt, updatedAt

- `id`*: number
- `name`*: string
- `description`?: string
- `createdAt`*: Date
- `updatedAt`*: Date

### SessionMessage
Required:
id, sessionId, role, content, metadata, createdAt

- `id`*: number
- `sessionId`*: number
- `role`*: MessageRole
- `content`*: string
- `tokens`?: number
- `metadata`*: unknown
- `createdAt`*: Date

### Session
Required:
id, modelType, modelName, provider, status, metadata, createdAt

- `id`*: number
- `kbId`?: number
- `modelType`*: ModelType
- `modelName`*: string
- `provider`*: string
- `status`*: SessionStatus
- `error`?: string
- `metadata`*: unknown
- `createdAt`*: Date
- `messages`?: SessionMessage[]

