# PostMem 技术选型文档

## 项目概述

PostMem 是一个个人知识库系统，核心目标是实现高精度语义检索，同时保障数据隐私主权。系统支持本地嵌入向量生成和可配置的大模型文本切割策略。

---

## 一、前端技术栈

### 1.1 框架与架构

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **Next.js** | 14.2.20 | 全栈 React 框架，支持 SSR/SSG，提供 API 路由能力，简化部署流程 |
| **React** | 18.3.1 | 主流前端框架，组件化开发，生态成熟 |
| **App Router** | - | Next.js 现代路由架构，支持 React Server Components，更好的性能和开发体验 |

**架构选择**：
- 主要使用 **App Router** 架构（`app/` 目录）
- API 路由保留使用 **Pages Router**（`pages/api/` 目录）
- Swagger 文档页面使用 Pages Router（兼容性考虑）

### 1.2 UI 组件库

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **Ant Design** | 6.3.7 | 企业级 UI 组件库，组件丰富、设计规范完善，适合管理后台场景 |
| **@ant-design/icons** | 6.2.3 | Ant Design 官方图标库，图标风格统一 |
| **@ant-design/nextjs-registry** | 1.3.0 | Ant Design 与 Next.js App Router 的集成方案，解决样式注入问题 |

### 1.3 类型安全

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **TypeScript** | 5.6.3 | 强类型语言，提供完整的类型安全保障，减少运行时错误 |
| **Zod** | 3.23.8 | TypeScript-first 的 schema 验证库，用于 API 输入验证和类型推导 |

**TypeScript 配置**：
- 严格模式开启（`strict: true`）
- 模块解析使用 bundler 模式
- 路径别名 `@/*` 映射到项目根目录

---

## 二、后端技术栈

### 2.1 数据库层

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **PostgreSQL** | 18 | 成熟的关系型数据库，稳定性高，扩展性强 |
| **pgvector** | - | PostgreSQL 向量扩展，支持向量存储和相似度检索，无需引入专用向量数据库 |
| **PGRoonga** | - | PostgreSQL 全文检索扩展，内置 TokenBigram 分词器，原生支持中日韩文本 |
| **Prisma** | 7.8.0 | 现代 ORM，类型安全，迁移管理便捷，查询 API 直观 |

**数据库设计要点**：
- 使用 `vector(1024)` 类型存储嵌入向量（1024 维）
- HNSW 索引实现毫秒级向量检索
- PGRoonga TokenBigram 分词器实现中日韩全文检索
- 支持多知识库隔离（`kbName` 字段）
- 完整的元数据存储（JSON 类型）

**核心数据表**：
- `memories` - 内存片段存储（向量 + 内容 + 全文索引）
- `knowledge_bases` - 知识库
- `topics` - 知识库主题分类
- `vendors` - 厂商定义（LangChain 类映射 + 动态工厂代码）
- `provider_options` - 厂商可选配置项
- `providers` - 用户创建的提供商实例（关联厂商 + API Key）
- `models` - 具体模型配置
- `sessions` - 大模型调用会话记录
- `session_messages` - 会话消息详情

### 2.2 依赖注入

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **Awilix** | 10.0.2 | 轻量级 DI 容器，支持多种注入模式，适合 Node.js 应用 |

**DI 配置**：
- 使用 PROXY 注入模式（支持属性访问拦截）
- 所有服务使用 `scoped` 生命周期（请求作用域）
- Prisma 客户端使用 `asValue` 注册（连接池线程安全）

**核心服务**：
- `EmbeddingService` - 嵌入向量生成
- `CutModelService` - 文本切割分析
- `KBService` - 知识库核心逻辑
- `VendorService` - 厂商管理（CRUD + 动态工厂代码执行）
- `VendorProtocolService` - 厂商协议层（LangChain 类注册与模型实例化）
- `ProviderService` - 提供商实例管理
- `ProviderValidateService` - 提供商连接验证
- `ModelService` - 模型管理
- `SessionService` - 会话记录
- `SettingService` - 应用设置

### 2.3 LLM 集成

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **LangChain** | 1.4.0 | LLM 应用开发框架，提供统一的模型接口，简化多模型集成 |
| **Ollama** | latest | 本地大模型运行平台，支持 GPU 加速，保障数据隐私 |

**模型支持**：
- **嵌入模型**：Ollama (bge-m3)、OpenAI、自定义端点
- **切割模型**：Ollama、OpenAI、Anthropic、DeepSeek、自定义端点
- **动态厂商扩展**：通过数据库存储工厂代码，运行时 vm2 沙箱执行，无需发版即可新增厂商

**LangChain 使用**：
- `ChatOpenAI` - OpenAI/兼容端点集成（Google Gemini、通义千问、Kimi 等均兼容）
- `ChatAnthropic` - Anthropic Claude 集成
- `ChatOllama` - Ollama 本地模型集成
- `OpenAIEmbeddings` / `OllamaEmbeddings` - 嵌入模型
- 自定义工厂代码（如 DeepSeek Reasoning 模型）通过 `VendorProtocolService` 动态注册
- 支持结构化 JSON 输出（`format: 'json'`）

---

## 三、AI 模型选型

### 3.1 嵌入模型

| 模型 | 来源 | 选型理由 |
|------|------|----------|
| **bge-m3** | Ollama | 多语言嵌入模型，中文效果优秀，1024 维向量，支持本地运行保障隐私 |

**特点**：
- 1024 维向量维度
- 多语言支持（中英文效果均佳）
- 完全本地生成，数据不外泄
- 百万级向量毫秒级检索

### 3.2 切割模型

| 模型 | 来源 | 选型理由 |
|------|------|----------|
| **mistral:7b** | Ollama | 轻量级本地模型，JSON 输出稳定，适合文本分析任务 |
| **gpt-4o-mini** | OpenAI | 云端备选方案，处理复杂文本分析 |
| **claude-3-5-sonnet** | Anthropic | 云端备选方案，推理能力强 |

**配置灵活性**：
- 默认推荐本地模式（数据隐私）
- 可切换云端模型（处理复杂场景）
- 支持自定义端点（兼容其他模型服务）

---

## 四、部署与运维

### 4.1 容器化

| 技术 | 选型理由 |
|------|----------|
| **Docker** | 标准化部署环境，简化依赖管理 |
| **Docker Compose** | 多服务编排，一键启动完整环境 |

**容器架构**：
- `postgres` - PostgreSQL + pgvector + PGRoonga 数据库
- `ollama` - 本地大模型服务（支持 GPU）
- `app` - Next.js 应用

### 4.2 包管理

| 技术 | 选型理由 |
|------|----------|
| **pnpm** | 高效的包管理器，磁盘空间占用少，依赖管理严格 |

**优势**：
- 符号链接存储，节省磁盘空间
- 严格的依赖解析，避免幽灵依赖
- 支持 monorepo 架构

### 4.3 运行环境

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **Node.js** | ≥18.0.0 | LTS 版本，性能稳定，支持最新 ES 特性 |
| **Alpine Linux** | - | Docker 基础镜像，体积小，安全性高 |

---

## 五、开发工具

### 5.1 代码质量

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **ESLint** | 8.57.0 | JavaScript/TypeScript 代码检查工具 |
| **eslint-config-next** | 14.2.20 | Next.js 官方 ESLint 配置 |

### 5.2 API 文档

| 技术 | 版本 | 选型理由 |
|------|------|----------|
| **Swagger UI** | 5.18.2 | OpenAPI 规范可视化展示，交互式 API 文档 |
| **OpenAPI 3.0** | - | RESTful API 标准规范，机器可读的 API 定义 |

---

## 六、架构设计原则

### 6.1 分层架构

```
┌─────────────────────────────────────┐
│  前端层 (Next.js App Router)         │
│  - 页面组件                          │
│  - UI 组件 (Ant Design)              │
└─────────────────────────────────────┘
            ↓ HTTP API
┌─────────────────────────────────────┐
│  API 层 (Pages Router)               │
│  - 路由处理                          │
│  - 输入验证 (Zod)                    │
│  - 异常处理                          │
└─────────────────────────────────────┘
            ↓ DI 容器
┌─────────────────────────────────────┐
│  服务层 (Awilix DI)                  │
│  - EmbeddingService                  │
│  - CutModelService                   │
│  - KBService                         │
└─────────────────────────────────────┘
            ↓ Prisma ORM
┌─────────────────────────────────────┐
│  数据层 (PostgreSQL + pgvector +     │
│          PGRoonga)                   │
│  - 向量存储                          │
│  - 全文检索（CJK 原生支持）          │
│  - 关系数据                          │
└─────────────────────────────────────┘
```

### 6.2 设计模式

| 模式 | 应用场景 |
|------|----------|
| **依赖注入** | 服务解耦，便于测试和替换实现 |
| **策略模式** | 多模型提供商切换（local/openai/custom） |
| **仓储模式** | 数据访问抽象，Prisma 作为实现 |
| **工厂模式** | LangChain 模型实例创建 |
| **动态工厂** | 数据库存储厂商工厂代码，运行时沙箱执行，无需发版扩展厂商 |

### 6.3 核心特性实现

**混合检索（Dense + Sparse + RRF）**：
- Dense 路：pgvector `<=>` 余弦距离算子，阈值 0.3 过滤低相关结果
- Sparse 路：PGRoonga `&@` 算子 + TokenBigram 分词器，原生支持中日韩文本分词，`pgroonga_score()` 相关性评分
- 融合算法：Reciprocal Rank Fusion（RRF），K=60，双路并行取 topK×3 后融合截断
- 评分策略：Dense/Hybrid 命中用余弦相似度，仅 Sparse 命中用 pgroonga_score 归一化值

**上下文窗口**：
- 检索时按 `ingest_batch` + `chunk_index` 定位同批次相邻片段
- 返回 prev/next 原文数组，还原完整推理上下文

**数据隐私保障**：
- 嵌入向量强制本地生成（bge-m3 + Ollama）
- 切割模型可选本地或云端
- 嵌入编码过程不落库（向量数据无审计价值）

---

## 七、技术选型权衡

### 7.1 PostgreSQL vs 专用向量数据库

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| PostgreSQL + pgvector + PGRoonga | 统一技术栈，运维简单，关系数据+向量+全文一体化 | 大规模向量性能不如专用方案 | ✅ 采用 |
| Pinecone/Milvus | 大规模向量性能更优 | 引入新技术栈，运维复杂度高 | ❌ 不采用 |

**决策理由**：个人知识库场景向量规模可控（百万级），pgvector 性能足够；PGRoonga 提供原生 CJK 全文检索，避免引入额外技术栈。

### 7.2 本地模型 vs 云端模型

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| 本地 Ollama | 数据隐私，无 API 成本 | 需要 GPU 资源，模型能力受限 | ✅ 默认采用 |
| 云端 API | 模型能力更强，无需本地资源 | 数据外泄，API 成本 | ⚠️ 可选配置 |

**决策理由**：嵌入模型强制本地保障隐私，切割模型提供云端选项应对复杂场景。

### 7.3 纯向量检索 vs 混合检索

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| 纯 Dense（pgvector） | 语义理解强，实现简单 | 关键词精确匹配漏召，专有名词召回差 | ❌ 不采用 |
| 混合（Dense + Sparse + RRF） | 语义+关键词互补，召回率 98.4% | 实现复杂度略高 | ✅ 采用 |

**决策理由**：知识库场景既有语义查询也有精确关键词查询（如人名、术语），混合检索覆盖两种模式，RRF 融合避免加权参数调优。PGRoonga TokenBigram 分词器原生支持中日韩文本，无需额外预处理。

### 7.4 PostgreSQL 内置全文检索 vs PGRoonga

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| PostgreSQL 内置 tsvector + simple 配置 | 无需额外扩展，开箱即用 | `simple` 配置器按空格分词，中日韩文本无法正确分词 | ❌ 不采用 |
| PGRoonga + TokenBigram | 原生 CJK 分词，无需预处理，`&@` 算子直接匹配 | 需安装扩展，索引占用略高 | ✅ 采用 |

**决策理由**：PostgreSQL 内置 `simple` 配置器按空格分词，中日韩文本连写无空格导致整句作为一个 token，无法有效检索。PGRoonga 的 TokenBigram 按 2 字符滑窗分词，天然适配 CJK 文本，且无需对入库内容做预处理。

### 7.5 Prisma vs 原生 SQL

| 方案 | 优势 | 劣势 | 决策 |
|------|------|------|------|
| Prisma ORM | 类型安全，迁移管理，API 直观 | 复杂查询受限 | ✅ 主要采用 |
| 原生 SQL | 完全控制，性能优化灵活 | 无类型安全，维护成本高 | ⚠️ 特殊场景使用 |

**决策理由**：常规操作使用 Prisma 保障类型安全，向量检索等特殊操作使用 `$queryRaw` 执行原生 SQL。

---

## 八、性能优化策略

### 8.1 向量检索优化

- HNSW 索引参数：`m=16, ef_construction=64`
- Dense 路阈值过滤 `cosine_distance < 0.3`，减少低相关结果进入融合
- 双路各取 topK×3，为 RRF 融合预留空间
- Sparse 路 PGRoonga 索引加速全文检索（TokenBigram 分词）

### 8.2 模型调用优化

- 模型配置缓存（避免重复数据库查询）
- 嵌入编码纯计算不落库，减少 I/O 开销

### 8.3 前端优化

- Next.js SSR/SSG 减少客户端渲染压力
- Ant Design 按需加载组件
- React 18 并发特性提升交互响应

---

## 九、安全与隐私

### 9.1 数据安全

- 嵌入向量本地生成，不传输到第三方
- API Key 等敏感信息存储在数据库，不暴露在环境变量
- 数据库连接使用强密码，限制网络访问

### 9.2 应用安全

- TypeScript 严格模式减少类型错误
- Zod 验证所有 API 输入，防止注入攻击
- 生产环境建议添加认证中间件

### 9.3 部署安全

- `.env` 文件不提交版本控制
- Docker 容器网络隔离
- 生产环境使用 HTTPS

---

## 十、技术演进规划

### 10.1 短期优化

- 完成 PGRoonga 索引创建与历史数据迁移
- 实现模型健康检查和自动切换
- 增加检索结果重排序（提升精度）

### 10.2 中期扩展

- 支持多模态内容（图片、音频嵌入）
- 实现知识库权限管理
- 添加检索历史和推荐功能

### 10.3 长期演进

- 探索更高效的向量索引算法
- 支持分布式部署（大规模场景）
- 厂商生态扩展（社区贡献工厂代码）

---

## 总结

PostMem 技术选型遵循以下原则：

1. **隐私优先**：嵌入向量强制本地生成，编码过程不落库
2. **类型安全**：TypeScript + Prisma + Zod 全栈类型保障
3. **架构简洁**：统一技术栈（PostgreSQL 同时承担关系+向量+全文检索），减少运维复杂度
4. **灵活配置**：模型提供商可切换，动态厂商扩展无需发版
5. **检索优先**：混合检索（Dense + Sparse + RRF）兼顾语义与精确匹配，PGRoonga 原生 CJK 分词保障中文检索质量

技术栈成熟稳定，适合个人知识库场景，具备良好的扩展性和维护性。