# PostMem 需求文档

## 项目定位

PostMem 是一个**后端服务**，为真正的聊天应用提供记忆管理、Web 搜索和对话管理能力。它本身不直接面向终端用户聊天，而是通过 API 接口让聊天应用接入。

---

## 一、知识库管理

### 1.1 知识库的创建与删除

- 可以创建多个知识库，每个知识库有名称和描述
- 可以删除知识库，删除时其下所有记忆片段和主题一并删除

### 1.2 知识库统计

- 查看所有知识库的片段数量、最后更新时间
- 查看单个知识库的统计信息

---

## 二、知识入库

### 2.1 纯文本入库

- 将一段纯文本写入指定知识库
- 系统自动将文本切割为语义完整的片段，为每个片段生成标题并重写使其连贯自足
- 每个片段生成向量嵌入，存入数据库以供语义检索

### 2.2 对话消息入库

- 将一组对话消息（用户/助手角色）写入指定知识库
- 系统先对消息分组，识别每组消息讨论的独立话题
- 对每组消息的内容进行切割、重写、生成嵌入

### 2.3 入库流程的智能处理

- **文本切割**：在语义完整的位置（段落结束、话题转换处）切分，每个片段 200-1000 字符
- **去重**：新片段与已有记忆对比，判断是跳过（已覆盖）、合并（有增量信息）还是新建（全新内容）
- **主题归类**：每个片段归入已有主题或创建新主题，优先复用已有主题，避免碎片化
- **上下文窗口**：检索时自动返回匹配片段的前后相邻片段，还原完整推理上下文

### 2.4 入库进度反馈

- 入库过程通过 SSE 流式推送进度事件，调用方可实时获知当前处理到第几步、总共几步

---

## 三、知识检索

### 3.1 语义搜索

- 在指定知识库中，根据查询文本进行语义检索
- 返回最相关的片段列表，每个片段包含标题、内容、相似度分数、所属主题
- 支持设置返回数量（top_k）和上下文窗口大小

### 3.2 检索结果带上下文

- 每条检索结果可携带前后相邻片段的内容，帮助理解片段的完整语境

---

## 四、聊天对话管理

### 4.1 对话的创建与查询

- 对话与知识库无关，不关联 kbId
- 查询对话列表，分页浏览
- 查询单个对话详情，包含所有消息
- 删除对话

### 4.1.1 消息列表响应

消息列表（`GET /api/chat/messages`）和对话详情中的每条消息包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | string | 消息 ID |
| `conversationId` | string | 所属对话 ID |
| `role` | string | 角色（`system` / `user` / `assistant`） |
| `content` | string | 消息内容 |
| `tokens` | number | Token 数量 |
| `totalTokens` | number | 总 Token 数量 |
| `memoried` | boolean | 是否已被自动记忆写入知识库 |
| `metadata` | object | 元数据 |
| `createdAt` | string | 创建时间 |

**`memoried` 字段的作用**：
- 前端根据此字段决定是否显示"重发"按钮：`memoried=true` 的消息不可重发
- 已记忆消息不可重发的原因：消息内容已写入知识库成为记忆片段，删除消息会导致知识库中的记忆与对话历史不一致

### 4.2 对话无状态

- 对话没有状态概念，创建后即持久存在
- 不存在 pending/completed/failed 等状态，也不记录错误信息
- 对话只用于关联消息和知识库，是一个纯粹的容器

### 4.3 会话（Session）与对话（Conversation）的区别

- **对话（Conversation）**：用户聊天产生的对话，由 `ConversationService` 管理，面向聊天场景，无状态（见 4.2 节）
- **会话（Session）**：入库流程中系统与 LLM 交互产生的**调试会话**，由 `SessionService` 管理，面向数据处理场景
- 入库流程（文本切割、消息分组、去重判断、主题匹配等）每次调用 LLM 时会创建一个 Session，记录 system/user/assistant 消息，用于追溯和调试 LLM 调用过程
- Session 与 Conversation 是独立的两个概念，互不依赖

#### Session 是有状态的

Session 不同于 Conversation 的无状态设计，它具有明确的生命周期和状态机：

- **状态流转**：`pending` → `completed` | `failed`
  - `pending`：Session 创建时的初始状态，表示 LLM 调用正在进行中
  - `completed`：LLM 调用成功完成，已收到完整响应
  - `failed`：LLM 调用失败，`error` 字段记录错误信息
- **错误记录**：失败的 Session 通过 `error` 字段记录错误详情，便于排查入库流程中的 LLM 调用问题
- **统计支持**：管理后台可按状态统计 Session 数量，监控入库流程的健康状况

---

## 五、聊天完成（核心接口）

### 5.1 发起聊天

- 调用方必须指定 `modelId`（使用哪个聊天模型）和 `kbId`（使用哪个知识库）
- 支持两种模式：
  - **新对话**：`newConversation=true`，系统创建新对话
  - **继续对话**：传入 `conversationId`，在已有对话上追加消息
- **默认行为**：既未传 `conversationId` 也未传 `newConversation=true` 时，获取最新已有对话继续聊天；若无任何对话则自动创建新对话
- 支持消息重发：传入 `regenerateMessageId`，删除该消息之后的所有消息，重新生成回复
- **已记忆消息不可重发**：如果 `regenerateMessageId` 对应的消息已被记忆（`memoried=true`），接口返回 400 错误，提示"已记忆的消息不可重发"。原因：已记忆的消息内容已写入知识库，删除消息会导致数据不一致

### 5.2 聊天流程

1. **保存用户消息**：将用户发送的消息存入对话
2. **自动记忆**：检查对话中未记忆的消息，当累计 token 超过阈值时，自动将旧消息写入知识库
3. **搜索需求分析**：AI 分析当前问题是否需要联网搜索和/或记忆搜索
4. **联网搜索**（如需要）：通过 SearXNG 搜索引擎搜索互联网，获取网页内容，缓存搜索结果
5. **记忆搜索**（如需要）：在对话关联的知识库中语义检索相关记忆
6. **生成回复**：将搜索结果和记忆作为上下文，流式生成 AI 回复
7. **保存助手消息**：将 AI 回复存入对话

### 5.3 流式响应

- 聊天回复通过 **Redis Stream** 推送，SDK 通过 `XREAD` 消费（详见第十二章 SDK 架构）
- 服务端将流事件写入 Redis Stream，SDK 从 Stream 拉取并通过 `onEvent` 回调推送
- 流事件类型：

| type | 字段 | 说明 |
|------|------|------|
| `chunk` | `content`, `model: { id, name }` | LLM 流式输出片段 |
| `status` | `status` | 状态通知（`searchingWeb` / `searchingMemory` / `summarizing` / `memoryProgress`） |
| `messageId` | `role`, `id` | 用户/助手消息 ID 通知（`role` 为 `user` 或 `assistant`） |
| `usage` | `promptTokens`, `completionTokens` | Token 用量统计 |
| `error` | `message` | 错误信息 |
| `done` | - | 流结束 |

**事件顺序示例**：
```
chunk → chunk → ... → messageId(user) → status(searchingMemory) → ... → chunk → ... → messageId(assistant) → usage → done
```

### 5.4 取消聊天

- 支持取消正在进行的聊天，中断流式生成

---

## 六、自动记忆

### 6.1 触发条件

- 对话中存在未记忆的消息
- 未记忆消息的累计 token 数超过设定的阈值（默认 50K）

### 6.2 记忆策略

- 保留最近 3 条消息不记忆（当前上下文不需要记忆）
- 将更早的未记忆消息写入对话关联的知识库
- 写入后标记这些消息为已记忆，避免重复处理

---

## 七、模型与提供商管理

### 7.1 厂商管理

- 管理AI厂商（如 OpenAI、Anthropic、Ollama 等）
- 每个厂商有名称、URL、聊天模型类名、嵌入模型类名、工厂代码

### 7.2 提供商管理

- 在厂商下创建提供商实例，配置 API Key、Base URL 等
- 支持验证提供商连接是否可用
- 支持从提供商拉取可用模型列表

### 7.3 模型管理

- 在提供商下注册模型，标记模型类型（chat/embedding）
- 支持设置默认模型（每种类型一个默认）
- 模型可启用/禁用

---

## 八、Web 搜索

### 8.1 搜索需求判断

- AI 自动判断用户当前问题是否需要联网搜索
- 判断标准：实时信息、最新技术/产品、特定资源查找、用户明确要求搜索

### 8.2 搜索执行

- 通过 SearXNG 元搜索引擎执行搜索
- 搜索结果（网页标题、URL、正文）缓存到数据库
- 如果缓存中已有相关网页，先判断缓存是否足够，不足时才重新搜索

---

## 九、聊天设置

- 记忆上下文阈值：控制自动记忆触发的 token 累计阈值

---

## 十、应用设置

- 最大内容长度、默认检索数量（top_k）、默认上下文窗口大小、默认分页大小
- 可通过 API 读取和更新

---

## 十一、API 文档

- `public/api-reference.yaml` 由代码自动生成，禁止手动编辑

---

## 十二、客户端 SDK（@postmem/sdk）

PostMem 是后端服务，调用方通过 SDK 接入。SDK 核心定位：**为调用端提供对话模块的客户端能力**，其他模块（知识库管理、模型管理等）不属于 SDK 范围。

### 12.1 架构原理

PostMem 服务端的对话流程是异步的：调用方通过 HTTP API 触发对话后，服务端将流式响应事件写入 **Redis Stream**。SDK 通过 HTTP SSE 接口消费事件，无需直连 Redis：

1. **HTTP 触发**：`POST /api/chat/completions` 发起对话，服务端异步处理，返回 `conversationId`
2. **Redis Stream 中转**：服务端将流事件写入 Redis Stream，作为消息缓冲层
3. **HTTP SSE 消费**：SDK 通过 `GET /api/chat/stream` 接收服务端的 SSE 流式推送，**无需 Redis 连接**
4. **调用流程**：先调用 `chat()` 获取 conversationId，再调用 `consume(onEvent)` 开始接收事件；调用方可通过 `conversationId` 进行取消等操作

#### 为什么使用 Redis Stream 中转？

**核心目的：支持网络断连/刷新页面的消息恢复**

- 当客户端因网络波动、页面刷新等意外断连时，Redis Stream 中的消息不会丢失
- 客户端重连后，可从上次消费位置继续读取剩余消息，避免聊天内容缺失
- 相比直接推送（Server-Sent Events 直连），Redis Stream 提供了**持久化的消息缓冲能力**
- 这是设计上的有意选择，而非遗留架构

### 12.2 单会话模型

SDK 设计为**单会话顺序聊天**：

- 全局唯一一个 `StreamReader` 实例，所有对话共享
- 不支持多会话并行聊天，一次只处理一个对话
- `chat()` 发起对话返回 `conversationId`，调用方通过独立的 `consume(onEvent)` 方法接收流式事件

### 12.3 核心功能：聊天

```typescript
// 1. 发起聊天，获取 conversationId
const conversationId = await client.chat({
  messages: [{ id: '1', content: '什么是 RAG？' }],
  modelId: 'model-id',
  kbId: 'kb-id',
  newConversation: true,
})

// 2. 消费流事件 — 两种模式

// 模式 A：不传回调，直接返回 SSE Response（推荐用于 HTTP 路由）
export async function GET(request: Request) {
  return await client.consume(undefined, { signal: request.signal })
}

// 模式 B：传回调，实时处理每个事件，返回 ChatResult
const result = await client.consume((event) => {
  if (event.type === 'chunk') process.stdout.write(event.content)
})
```

- 必须指定 `modelId`（聊天模型）和 `kbId`（知识库）
- 支持新对话（`newConversation=true`）和继续对话（传入 `conversationId`）
- 支持消息重发（传入 `regenerateMessageId`），删除该消息之后的所有消息，重新生成回复
- **已记忆消息不可重发**：传入的 `regenerateMessageId` 对应的消息如果 `memoried=true`，接口返回 400 错误
- `consume(onEvent?, options?)` 通过 HTTP SSE 接收流事件：
  - **不传回调**：返回原生 `Response` 对象（含 ReadableStream + SSE headers），可直接作为 HTTP 路由返回值使用
    - 可选传入 `{ signal }`（如 `request.signal`）：客户端断开时自动停止读流 + 关闭 stream，避免资源浪费；内置 30s keep-alive 心跳防止中间代理断开空闲连接
  - **传回调**：通过 `onEvent` 实时推送每个事件，最终返回 `ChatResult { conversationId, fullContent, promptTokens, completionTokens }`
  - 收到 `done` 或 `error` 事件后 Promise resolve

### 12.4 流事件类型

SDK 从 HTTP SSE 接收的事件与服务端 `StreamEvent` 完全对齐，原始 JSON 直接透传，不做格式转换：

| type | 字段 | 说明 |
|------|------|------|
| `chunk` | `content`, `model` | LLM 流式输出片段 |
| `status` | `status` | 状态通知（searchingWeb / searchingMemory / summarizing / memoryProgress） |
| `messageId` | `role`, `id` | 用户/助手消息 ID 通知 |
| `usage` | `promptTokens`, `completionTokens` | Token 用量 |
| `error` | `message` | 错误 |
| `done` | - | 流结束 |

### 12.5 辅助功能

| 方法 | 说明 |
|------|------|
| `cancel(conversationId)` | 取消正在进行的对话 |
| `consume(onEvent?)` | 不传回调返回 `Response`（可直接用于 HTTP 路由）；传回调返回 `ChatResult` |
| `getMessages(conversationId, params?)` | 获取对话消息列表（分页） |
| `listConversations(params?)` | 获取对话列表（分页） |
| `getConversation(conversationId)` | 获取单个对话详情 |
| `createConversation(metadata?)` | 创建新对话 |
| `deleteConversation(conversationId)` | 删除对话 |

### 12.6 配置

```typescript
new PostMemClient({
  baseUrl: 'http://localhost:3000',  // PostMem 服务端地址
  requestTimeout: 30_000,            // HTTP 请求超时（ms），默认 30s
  streamTimeout: 300_000,            // 流式连接超时（ms），默认 5min
})
```

### 12.7 前置条件

- 调用方需能访问 PostMem 服务端的 HTTP 接口
- 运行环境需支持原生 `fetch` 和 ReadableStream（Node.js 18+ 或现代浏览器）

---

## 十三、管理后台

- 仪表盘：展示知识库总数、片段总数、各知识库概览
- 知识库列表：浏览和管理知识库及其片段
- 搜索测试：在知识库中测试语义检索效果
- 模型管理：管理厂商、提供商、模型
- 对话管理：查看对话历史和消息
- 设置：调整应用参数
