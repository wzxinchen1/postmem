# 知识库系统核心原理

## 一句话总结

> **"Store everything, then make it findable."** — 存储一切原文，不总结不提取，把检索问题单独解决。

---

## 核心哲学：Verbatim-First（原文优先）

### 与传统方案的对比

| 对比维度 | 传统方案（Mem0/Zep/LangMem） | 本系统方案 |
|----------|------------------------------|-----------|
| **写入方式** | 每次调用 LLM 提取/总结事实 | LLM 切分+重写，原文完整存储 |
| **存储内容** | 结构化事实/实体/三元组（有损压缩） | 原始文本 verbatim（无损保留） |
| **召回率** | ~85% | **96.6%**（纯向量） / **98.4%**（混合检索）|

### 为什么选择 Verbatim？

**根本性洞察：你无法预测未来会问什么。**

```
用户输入："我下周二要去见张医生。"

提取式系统 → 存入："用户有医疗预约"
             （丢弃了"张医生""下周二"等细节）

三个月后用户问："我上次见张医生是什么时候？"
→ 提取式系统：❌ 找不到（已被清除或模糊化）
→ Verbatim 系统：✅ 原文完整保存，向量检索命中
```

---

## 入库流水线完整流程

### 架构总览

```
┌─────────────────────────────────────────────────────────────┐
│  用户提交文本                                                │
│     ↓                                                        │
│  ┌──────────┐    SSE 流式响应    ┌─────────────────────────┐ │
│  │  前端 UI  │ ──────────────→   │   API Route (SSE)       │ │
│  └──────────┘                   │   pages/api/kb/ingest   │ │
│                                 └────────────┬────────────┘ │
│                                              ↓               │
│                                 ┌─────────────────────────┐ │
│                                 │  KBService              │ │
│                                 │  .ingestTextStream()     │ │
│                                 └────────┬────────────────┘ │
│                                          ↓                  │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                    四阶段处理流程                         │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │                                                         │ │
│  │  ① 文本切分          ② 主题规划        ③ 逐块去重入库   │ │
│  │     ↓                    ↓                 ↓           │ │
│  │  LLM: cutAndRewrite   LLM: batchResolve  Embedding      │ │
│  │                      Topics            + searchInTopic   │ │
│  │                                          ↓               │ │
│  │                                    LLM: shouldIngest    │ │
│  │                                      Chunk (skip/merge/  │ │
│  │                                       new)               │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                          ↓                   │
│                              返回 { count, memoryIds, topicsInvolved }
└─────────────────────────────────────────────────────────────┘
```

### 阶段一：文本切分 (LLM 调用 ×1)

**方法**: `CutModelService.cutAndRewrite(content, kbId)`

```
输入: 用户原始长文本（最长 maxContentLength 字符）
         ↓
LLM 将文本切分为语义完整、连贯的片段
每个片段包含:
  - index: 片段序号
  - title: 片段标题（概括该片段内容）
  - content: 完整原文（不丢失任何信息）
         ↓
输出: TitledChunk[] （通常 5-15 个片段）
```

**关键特性**：
- 切分和重写标题由 LLM 一步完成
- 每个片段语义独立、边界清晰
- **原文完整保留**，不做摘要或压缩

### 阶段二：主题规划 (LLM 调用 ×1~N)

这是两步操作：

#### 2a. 批量主题归属规划

**方法**: `CutModelService.batchResolveTopics(chunks, existingTopics)`

```
输入:
  - 所有切片的标题列表
  - 已有主题列表（名称+描述）
         ↓
LLM 为每个切片决定:
  - action: "select" (归入已有主题) 或 "create" (需要新主题)
  - topicName / newTopicName: 目标主题名
         ↓
输出: BatchTopicPlan { plans: ChunkTopicPlan[] }
```

**提示词规则**：
- 同源文档通常属于 **1-2 个大主题**
- 新主题名称**不超过 5 个字**（如 "NLR"、"部署"、"认证"）
- 优先使用大类领域名作为主题名

#### 2b. 新主题合并去重（如有创建请求）

**方法**: `CutModelService.batchCreateTopics(proposedTopics)` 

```
问题场景:
  8 个切片都建议 create → 可能拟出 8 个不同名字的主题
  但实际它们可能都属于同一领域！

解决方案:
  收集所有拟建主题 + 各自的样本内容
  → 一次性发给 LLM 合并去重
  → 通常合并为 1-2 个最终主题

示例:
  输入: ["机制与验证", "系统架构", "概述与展望", 
         "架构设计", "数据与愿景", "机制与存储"]
  输出: [{ name: "NLR", description: "..." }]
  
  所有原始 plan 的 newTopicName 都映射到同一个 topic ID
```

#### 2c. 数据库写入

```sql
-- 创建新主题（仅最终合并后的）
INSERT INTO topics (kb_id, name, description) VALUES (...)

-- 建立 name → id 映射表
-- 每个 plan.newTopicName 都指向正确的 topicId
```

### 阶段三：逐块去重入库 (循环)

对每个 chunk 执行以下流程:

```
for each chunk in chunks:
  │
  ├── ① 解析主题归属（从阶段二的 plan 中获取 topicId）
  │
  ├── ② 全知识库范围相似度搜索
  │     └── searchInTopic(kbId, chunk.content)
  │         ├── Dense: 语义向量相似度 topN
  │         ├── Sparse: 关键词全文搜索 topN
  │         └── RRF 融合排序，返回 top3
  │
  │     关键: 过滤掉本次请求已产生的 ID（thisBatchIds）
  │     → 避免同批次内互相干扰
  │
  └── ③ 三分支判断
      │
      ├── [无相似] → 直接 INSERT
      │   - 生成 embedding
      │   - 写入 memories 表
      │   - 记录 ID 到 thisBatchIds
      │
      ├── [有相似] → LLM 判断: shouldIngestChunk()
      │   │
      │   ├── skip  → 跳过（重复内容）
      │   │
      │   ├── merge → 合并到已有记录
      │   │   - LLM 输出 mergedContent
      │   │   - 重新生成 embedding
      │   │   - UPDATE memories 表
      │   │   - 记录目标 ID 到 thisBatchIds
      │   │
      │   └── new   → 作为新记录插入
      │       - 生成 embedding
      │       - INSERT memories 表
      │       - 记录新 ID 到 thisBatchIds
      │
      └── 继续下一个 chunk
```

### LLM 调用统计

一次完整入库操作的 LLM 调用次数:

| 步骤 | 方法 | 调用次数 | 目的 |
|------|------|---------|------|
| 切分 | `cutAndRewrite()` | **×1** | 将长文本切成语义完整片段 |
| 主题规划 | `batchResolveTopics()` | **×1** | 为所有片段统一分配主题 |
| 主题生成 | `batchCreateTopics()` | **×1** | 合并去重，生成最终主题 |
| 去重判断 | `shouldIngestChunk()` | **×M** | M = 有相似记录的 chunk 数 |

**最少**: 3 次（无重复时）  
**最多**: 3 + N 次（N = chunk 总数，全部都有相似记录时）

### 数据存储结构

每条 Memory 记录:

```
INSERT INTO memories (
  kb_id,           -- 所属知识库
  topic_id,        -- 所属主题
  title,           -- LLM 生成的片段标题
  content,         -- 完整原文
  embedding,       -- 语义向量 (用于 Dense 检索)
  metadata,        -- JSONB: { cutModel: 'cut-and-rewrite' }
) VALUES (...)

-- 索引:
-- embedding: 向量索引 (Dense 检索)
-- content: 全文索引 (Sparse 检索)
-- (kb_id, topic_id): 复合索引 (按主题过滤)
```

---

## 流式响应机制

### 为什么需要 SSE?

入库是一个耗时操作（多个 LLM 调用 + 多次 DB 写入），传统 JSON 响应会导致：
- 用户等待时间过长，不知道进度
- 无法提前感知错误
- 体验差，容易误以为卡死

### SSE 事件流

```
前端 POST /api/kb/ingest
  ↓
服务端设置 Content-Type: text/event-stream
  ↓
实时推送事件:
  
data: {"type":"status",    "message":"正在切分文本..."}
data: {"type":"status",    "message":"文本已切分为 8 个片段，正在规划主题..."}
data: {"type":"status",    "message":"正在规划主题归属..."}
data: {"type":"status",    "message":"拟创建 8 个主题，正在合并去重..."}
data: {"type":"status",    "message":"创建主题：NLR"}
data: {"type":"status",    "message":"已合并为 1 个主题"}

data: {"type":"progress", "message":"处理片段 1/8：NLR定义与理论基石",
       "data":{"current":1,"total":8,"title":"..."}}

data: {"type":"chunk_detail","message":"检测相似内容...", "data":{"title":"..."}}
data: {"type":"chunk_detail","message":"无相似记录，直接入库", "data":{"action":"insert"}}

data: {"type":"progress", "message":"处理片段 2/8：...", ...}
...

data: {"type":"complete", "data":{"count":6,"memoryIds":[...],"topicsInvolved":["NLR"]}}
  ↓
连接关闭
```

### 前端展示

```
┌─────────────────────────────────────────────┐
│  🔄 正在切分文本...                          │
│                                             │
│  📝 文本已切分为 8 个片段，正在规划主题...    │
│                                             │
│  📂 创建主题：NLR                           │
│                                             │
│  ⚙️ 处理片段 3/8：NLR验证机制                │
│     ✅ 无相似记录，直接入库                  │
│                                             │
│  ⚙️ 处理片段 4/8：NLR系统架构                │
│     ~ 发现 2 条相似记录，进行去重判断...      │
│     ✅ 合并到已有记录                        │
│     ⚠️ 跳过（与已有记录重复）                │
│                                             │
│  ✅ 完成！创建了 6 个片段                     │
└─────────────────────────────────────────────┘
```

### 技术实现要点

**后端**:
```typescript
// API 路由
res.setHeader('Content-Type', 'text/event-stream')
res.setHeader('Cache-Control', 'no-cache, no-transform')
res.setHeader('X-Accel-Buffering', 'no')  // 禁止代理层缓冲

const sendEvent = (event) => {
  res.write(`data: ${JSON.stringify(event)}\n\n`)
  res.flush()  // 强制立即发送
}

// 服务层
await kbService.ingestTextStream(kbId, content, sendEvent)
```

**前端**:
```typescript
// fetch + ReadableStream 解析 SSE
await streamPost('/api/kb/ingest', data, (event) => {
  setIngestProgress(event)  // 实时更新 UI
}, abortSignal)
```

---

## 去重机制详解

### 设计原则

> **增量价值判断** — 由 LLM 决定新片段是跳过、合并还是新增

### 为什么不用简单去重？

```
方案 A: 内容完全匹配去重
  问题: LLM 重写后的文本可能措辞略有不同
        即使意思相同也会重复入库

方案 B: 相似度阈值自动过滤
  问题: 固定阈值无法适应所有场景
        有些高度相似但有增量的内容会被误杀

方案 C (当前): LLM 智能判断 ✓
  优势: 理解语义差异
        能识别真正的增量价值
        可控的 merge 行为（合并而非丢弃）
```

### 去重搜索范围

**重要**: 去重搜索是**全知识库范围**，不限于单个 topic

```
旧逻辑 (bug):
  WHERE kb_id = $1 AND topic_id = $2  ← 限制在当前 topic 内
  
  问题: 同一篇文章两次入库时，
        chunk 分配到不同 topic → 搜不到对方 → 重复入库

新逻辑 (修复):
  WHERE kb_id = $1  ← 整个知识库范围内搜索
  
  效果: 不管 chunk 属于哪个 topic，
        都能与已有数据做去重对比
```

### 本次批次隔离

```
thisBatchIds: Set<number>

每次 INSERT 或 UPDATE 后:
  memoryIds.push(id)
  thisBatchIds.add(id)

每次搜索前过滤:
  similarMemories = similarMemories.filter(m => !thisBatchIds.has(m.id))

效果: 
  chunk A 先入库 → 产生 ID 100
  chunk B 搜索时 → 排除 ID 100
  → chunk B 不会因为与 chunk A 相似而被跳过
  → 避免同批次内互相干扰
```

---

## 主题系统

### 设计理念

主题是对知识的**粗粒度分类**，用于:
- 组织相关联的记忆片段
- 检索时可限定主题范围
- 提供知识结构的宏观视图

### 主题命名规范

- **不超过 5 个字**（如 "NLR"、"部署"、"认证"）
- 使用大类领域名（避免细碎描述）
- 同一文档通常只产生 **1-2 个主题**

### 主题生命周期

```
文档首次入库:
  batchResolveTopics() → 拟定 N 个主题名
  batchCreateTopics()  → 合并为 M 个（M ≤ N）
  CREATE topics        → 写入数据库
  映射关系建立         → 每个 plan 指向正确 topicId

后续入库同一知识库:
  已有主题直接复用
  只有明显不属于任何现有主题时才创建新的
```

---

## 混合检索实现（Dense + Sparse + RRF）

### Dense 路（语义搜索）

- **原理**: 将文本转为向量，通过余弦距离衡量语义相似度
- **预取**: topK × 3 条（为 RRF 融合预留空间）

### Sparse 路（关键词搜索）

- **原理**: 对原文建立全文索引，通过关键词匹配召回
- **预取**: topK × 3 条

### RRF 融合

- **公式**: `score = Σ 1 / (K + rank_i)`，K = 60
- **来源标记**: 同一 ID 在两路都命中时标记为 `hybrid`
- **评分策略**:

| 来源 | score 计算 |
|------|-----------|
| Dense / Hybrid | `cosine_similarity`（0.7 ~ 1.0）|
| 仅 Sparse | 全文搜索得分归一化值（0 ~ 1）|

### 上下文窗口

- 返回结果可携带相邻 chunk 的原文（prev / next）
- 按 `ingest_batch` + `chunk_index` 定位同批次邻居
- 窗口大小由调用方指定

---

## 性能指标

| 配置方案 | LongMemEval R@5 | LLM 调用 |
|----------|-----------------|----------|
| 纯语义搜索（raw verbatim） | **96.6%** | 仅写入时 |
| 混合检索（Hybrid v4） | **98.4%** | 仅写入时 |

---

## 设计原则

1. **消除提取损失** — 无压缩 = 无信息丢失
2. **可审计性** — 任何记忆都可追溯到原始来源
3. **可纠错性** — 发现问题时可以重新审视原文
4. **面向未来** — 不预设什么重要，让检索时决定
5. **宁缺毋错** — 错误的记忆比没有记忆更糟
6. **流式反馈** — 长耗时操作必须提供实时进度
7. **批量智能** — 主题创建要全局考虑，拒绝碎片化
8. **去重隔离** — 同批次数据互不干扰

---

## 错误处理策略

| 错误类型 | HTTP 状态码 | 处理方式 |
|---------|------------|---------|
| 内容为空 | 400 | 前端校验 + 后端双重检查 |
| 内容超长 | 400 | 提示具体限制值 |
| 知识库不存在 | 404 | 明确提示 |
| LLM 调用失败 | 503 | 返回具体错误原因 |
| 数据库异常 | 500 | 通用提示 + 日志记录 |
| 内部逻辑错误 | 500 | AppError.details 字段携带详情 |
