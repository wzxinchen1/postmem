# MemPalace 记忆系统核心原理

## 一句话总结

> **"Store everything, then make it findable."** — 存储一切原文，不总结不提取，把检索问题单独解决。

---

## 核心哲学：Verbatim-First（原文优先）

### 与传统方案的对比

| 对比维度 | 传统方案（Mem0/Zep/LangMem） | MemPalace 方案 |
|----------|------------------------------|----------------|
| **写入方式** | 每次调用 LLM 提取/总结事实 | 零 LLM 调用，原文直存 |
| **存储内容** | 结构化事实/实体/三元组（有损压缩） | 原始文本 verbatim（无损保留） |
| **成本模式** | 按 token 付费，每次写入有 API 成本 | 完全免费 |
| **召回率** | ~85% | **96.6%**（纯向量） / **98.4%**（混合检索） |

### 为什么选择 Verbatim？

**根本性洞察：你无法预测未来会问什么。**

```
用户输入："我下周二要去见张医生。"

提取式系统 → 存入："用户有医疗预约"
             （丢弃了"张医生""下周二"等细节）

三个月后用户问："我上次见张医生是什么时候？"
→ 提取式系统：❌ 找不到（已被清除或模糊化）
→ Verbatim 系统：✅ 原文完整保存，向量检索命中
```

---

## 摘要陷阱（为什么不能做摘要）

### 第一层：压缩是有损且不可预测的

LLM 提取时几乎一定会丢失：
- ❌ 为什么排除某方案（隐含的场景适配信息）
- ❌ 被提及但排除的备选方案（团队能力边界暗示）
- ❌ 决策的时间/项目阶段（时间约束）
- ❌ 谁参与了讨论（责任归属）

> **比喻**："结论是肌肉，推理链是骨架。没有骨架的肌肉是一团无法站立的肉。"

### 第二层：提取错误是静默的

比信息丢失更严重——**错误记忆比无记忆更危险**：
- 无记忆 AI → 每次从零开始，烦但不会出错
- 错误记忆 AI → 基于错误前提给出建议，且表现得自信，用户容易信服

真实案例：
```
原始对话："团队选了 Postgres，但 Maya 更倾向 MongoDB..."
错误提取："Maya recommended Postgres based on Acme project experience"
→ 完全颠倒了立场！且会被当作"事实"使用
```

### 第三层：原始记录不可恢复

这是一个**单向门（one-way door）**：
- ❌ 无法审计：这条记忆来自哪段原文？
- ❌ 无法纠错：不能查看原文重新提取
- ❌ 无法补充：当时的讨论还提到了什么？

> 数据工程视角的反模式：只保留派生数据（derived data），丢弃原始数据（raw data）。

---

## 正确问题 vs 错误问题

| 错误问题 | 正确问题 |
|----------|---------|
| "什么值得记住？"（需要预测未来需求，这不可能） | "**如何在保留一切的前提下，使检索高效？**" |

### 核心转变

```
传统方案：智能提取（有损压缩）→ 存储少量摘要
MemPalace：完整存储（零丢失）+ 结构化检索（真正的工程挑战）
```

---

## 技术架构

### 写入流水线（零 LLM 调用）

```
① 文本分块（固定策略，纯字符串操作）
       ↓
② 嵌入编码（本地模型，如 all-MiniLM-L6-v2）
       ↓
③ 向量化存储（pgvector / ChromaDB / LanceDB）
       ↓
④ [可选] 关键词提取（非 LLM）→ 辅助混合检索

✗ 全程无 LLM API 调用
✗ 全程无 token 费用
✗ 确定性输出（同一输入 = 同一输出）
```

### 向量的角色：索引而非内容

**关键认知：向量不是记忆本身，而是查找记忆的索引。**

```
┌─────────────────────────────────────────┐
│              检索流程                    │
├─────────────────────────────────────────┤
│                                         │
│  用户查询                               │
│     ↓                                   │
│  查询向量化（纯计算，不落库）             │
│     ↓                                   │
│  Dense + Sparse 双路并行检索            │
│     ↓                                   │
│  RRF 融合排序                           │
│     ↓                                   │
│  返回完整原文 + 上下文窗口               │
│                                         │
└─────────────────────────────────────────┘

向量索引          原文存储
┌──────┐         ┌──────────────────┐
│ id=1 │ ──────→ │ 完整原文内容...   │
│ id=2 │ ──────→ │ 完整原文内容...   │
│ id=3 │ ──────→ │ 完整原文内容...   │
└──────┘         └──────────────────┘
```

### 混合检索实现（Dense + Sparse + RRF）

系统采用双路并行检索，通过 Reciprocal Rank Fusion 融合：

**Dense 路（语义搜索）：**
- 算子：`<=>` 余弦距离
- 阈值过滤：`cosine_distance < 0.3`
- 取 topK × 3 条为融合预留空间

**Sparse 路（关键词搜索）：**
- 引擎：PostgreSQL 全文搜索（`tsvector` + `ts_rank`）
- 配置器：`simple`（适配中英文混合场景）
- 取 topK × 3 条为融合预留空间

**RRF 融合：**
- 公式：`score = Σ 1 / (K + rank_i)`，K = 60
- 同一 ID 在两路都命中时标记为 `hybrid` source

**最终评分策略：**
| 来源 | score 计算 |
|------|-----------|
| Dense / Hybrid | `cosine_similarity`（0.7 ~ 1.0）|
| 仅 Sparse | `ts_rank` 归一化值（0 ~ 1）|

**上下文窗口：**
- 返回结果可携带相邻 chunk 的原文（prev / next）
- 按 `ingest_batch` + `chunk_index` 定位同批次邻居
- 窗口大小由调用方指定

**向量化服务约束：**
- 嵌入编码过程不写入会话表（向量数据不可读，无审计价值）
- 只做一件事：输入文本 → 输出向量数组

---

## 性能数据

| 配置方案 | LongMemEval R@5 | LLM 调用 |
|----------|-----------------|----------|
| 纯语义搜索（raw verbatim） | **96.6%** | 零调用 |
| 混合检索（Hybrid v4） | **98.4%** | 零调用 |
| 混合 + Rerank | **≥99%** | 可选（小模型即可） |

> 对比：Mem0 最初 ~49%，2026年4月提升至 93.4%；Zep ~85%

---

## 设计原则

1. **消除提取损失** — 无压缩 = 无信息丢失
2. **可审计性** — 任何记忆都可追溯到原始来源
3. **可纠错性** — 发现问题时可以重新审视原文
4. **面向未来** — 不预设什么重要，让检索时决定
5. **宁缺毋错** — 错误的记忆比没有记忆更糟

---

## 参考资源

- **GitHub**: https://github.com/milla-jovovich/mempalace
- **论文批判**: arXiv:2604.21284 — "Spatial Metaphors for LLM Memory: A Critical Analysis of the MemPalace Architecture"
- **协议**: MIT
- **系统要求**: 本地运行，无需 API Key
