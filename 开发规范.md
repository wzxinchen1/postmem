# 开发规范

## 核心原则

**除明确允许的做法外，其他做法全部禁止。遇到无法完成的任务时，立即停止并通知用户。**

---

## 强制规则

### 1. 依赖注入必须用解构赋值

**要求**: 构造函数必须用解构赋值,禁止 TypeScript 简写语法。

**原因**: Awilix PROXY 模式会拦截属性访问,简写语法会导致 `this.prisma.provider` 被误解析为依赖项。

**允许的做法**:
```typescript
constructor({ prisma }: { prisma: PrismaClient }) {
  this.prisma = prisma
}
```

**禁止的做法**:
```typescript
constructor(private prisma: PrismaClient) {}
constructor(protected prisma: PrismaClient) {}
constructor(public prisma: PrismaClient) {}
```

---

### 2. 实现代码禁止注释

**要求**: 方法内部禁止任何注释。

**允许的做法**:
- 公共方法上的 JSDoc 文档注释
- 复杂正则表达式后的单行说明注释
- 配置文件中的说明注释

**允许的示例**:
```typescript
/**
 * 获取用户列表
 */
async listUsers() {
  return this.prisma.user.findMany({ where: { isActive: true } })
}

const jsonMatch = jsonStr.match(/```(?:json)?\s*([\s\S]*?)```/)  // 提取 markdown 代码块中的 JSON
```

**禁止的做法**:
```typescript
async listUsers() {
  // 查询活跃用户
  return this.prisma.user.findMany({ where: { isActive: true } })
}

async processData() {
  /* 这里处理数据 */
  const data = await this.fetchData()
  // TODO: 优化性能
  return this.transform(data)
}
```

---

### 3. 服务层禁止 try-catch

**要求**: 服务层直接抛异常,由 API 层的 `withErrorHandler` 统一处理。

**允许的做法**:
- 服务层让异常自然抛出
- API 层使用 `withMiddleware` 包装
- 前端代码使用 try-catch 处理网络错误

**允许的示例**:
```typescript
// ✅ 服务层 - 让异常自然抛出
async someOperation() {
  return this.prisma.someModel.findMany()
}

// ✅ API 层 - 统一处理
export default withMiddleware(async (req, res) => {
  const result = await service.someOperation()
  successResponse(res, result)
}, { methods: ['GET'] })

// ✅ 前端 - 处理网络错误
const loadData = async () => {
  try {
    const res = await fetch('/api/data')
    setData(await res.json())
  } catch (error) {
    showMessage('error', '加载失败')
  }
}
```

**禁止的做法**:
```typescript
// ❌ 服务层使用 try-catch
async someOperation() {
  try {
    return this.prisma.someModel.findMany()
  } catch (error) {
    throw Errors.databaseError(error.message)
  }
}

// ❌ 服务层捕获后重新抛出
async anotherOperation() {
  try {
    await this.doSomething()
  } catch (e) {
    console.error(e)
    throw e
  }
}
```

---

### 4. 异常分类与响应规范

**要求**: 业务异常和用户输入导致的异常统一响应 400 错误,响应格式与 500 错误一致。

**异常分类**:
- **业务异常**: 业务逻辑校验失败(如资源不存在、状态不满足条件等)
- **用户输入异常**: 参数校验失败(如必填项缺失、格式错误、值范围错误等)
- **系统异常**: 数据库错误、外部服务不可用等(响应 500/503)

**响应格式**:
所有异常统一使用 `AppError` 类,通过 `withErrorHandler` 自动处理:
```typescript
{
  success: false,
  error: {
    code: "ERROR_CODE",
    message: "错误描述",
    details?: "详细信息"
  }
}
```

**允许的做法**:
```typescript
// ✅ 用户输入异常 - 使用 badRequest
if (!name || name.trim().length === 0) {
  throw Errors.badRequest('知识库名称不能为空')
}

if (name.length > 100) {
  throw Errors.badRequest('知识库名称不能超过 100 个字符')
}

if (!['public', 'private'].includes(visibility)) {
  throw Errors.badRequest('可见性必须是 public 或 private', `实际值: ${visibility}`)
}

// ✅ 业务异常 - 使用 badRequest
const existingKB = await this.prisma.knowledgeBase.findUnique({ where: { name } })
if (existingKB) {
  throw Errors.badRequest('知识库名称已存在', `名称: ${name}`)
}

const kb = await this.prisma.knowledgeBase.findUnique({ where: { id } })
if (!kb) {
  throw Errors.projectNotFound(kbName)  // 404,特殊的业务异常
}

if (kb.status !== 'active') {
  throw Errors.badRequest('知识库未激活,无法执行此操作', `当前状态: ${kb.status}`)
}

// ✅ 系统异常 - 使用特定错误码
try {
  await this.embeddingService.embed(text)
} catch (error) {
  throw Errors.embeddingError(error.message)  // 503
}
```

**禁止的做法**:
```typescript
// ❌ 直接返回 JSON 响应而不抛出异常
if (!name) {
  return res.status(400).json({ success: false, error: { message: '名称不能为空' } })
}

// ❌ 使用 HTTP 状态码作为异常类型
throw new Error('400: 名称不能为空')

// ❌ 业务异常使用 500 错误码
if (!kb) {
  throw Errors.internalError('知识库不存在')  // 应该用 projectNotFound 或 badRequest
}

// ❌ 用户输入异常使用系统错误码
if (name.length > 100) {
  throw Errors.internalError('名称过长')  // 应该用 badRequest
}
```

**错误码使用规范**:
- `BAD_REQUEST` (400): 用户输入异常、一般业务异常
- `KB_NOT_FOUND` (404): 知识库不存在
- `MEMORY_NOT_FOUND` (404): 片段不存在
- `EMBEDDING_ERROR` (503): 嵌入模型服务不可用
- `CUT_MODEL_ERROR` (503): 切割模型服务不可用
- `DATABASE_ERROR` (500): 数据库操作异常
- `INTERNAL_ERROR` (500): 其他内部错误

**添加新错误码的流程**:
1. 在 `src/lib/errors.ts` 的 `ErrorCode` 枚举中添加新错误码
2. 在 `ErrorCodeToStatus` 中映射对应的 HTTP 状态码
3. 在 `Errors` 对象中添加工厂函数
4. 在服务层使用新错误码抛出异常

**前端异常处理规范**:
前端必须正确处理非 200 响应,根据 HTTP 状态码判断错误类型并使用 `message.info` 或 `message.error` 通知用户。

**后端响应格式规范**:
- **成功响应 (200)**: 返回 JSON 格式 `{ success: true, data: {...} }`
- **错误响应 (非 200)**: 返回纯文本格式 (`text/plain; charset=utf-8`),内容为错误消息

**允许的做法**:
```typescript
// ✅ 使用 useMessage hook 处理错误
const { contextHolder, showMessage } = useMessage()

const handleSubmit = async () => {
  try {
    const res = await fetch('/api/kb/create', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: kbName })
    })
    
    if (!res.ok) {
      const errorMessage = await res.text()  // 非 200 响应统一用 text() 解析
      if (res.status >= 400 && res.status < 500) {
        showMessage('info', errorMessage)  // 业务异常和输入错误用 info
      } else {
        showMessage('error', '操作失败')  // 系统错误用 error
      }
      return
    }
    
    const data = await res.json()  // 200 响应用 json() 解析
    if (data.success) {
      showMessage('success', '创建成功')
    }
  } catch (err) {
    showMessage('error', '网络请求失败')
  }
}

// ✅ 使用 antd message API (在组件内)
const [msg, contextHolder] = message.useMessage()

const handleDelete = async (id: number) => {
  try {
    const res = await fetch(`/api/kb/delete?kbName=${kbName}`, {
      method: 'DELETE'
    })
    
    if (!res.ok) {
      const errorMessage = await res.text()  // 统一用 text() 解析错误响应
      if (res.status >= 400 && res.status < 500) {
        msg.info(errorMessage)  // 业务异常用 info
      } else {
        msg.error('删除失败')  // 系统错误用 error
      }
      return
    }
    
    const data = await res.json()  // 成功响应用 json() 解析
    msg.success('删除成功')
  } catch (err) {
    msg.error('网络请求失败')
  }
}
```

**禁止的做法**:
```typescript
// ❌ 不判断响应状态,直接解析 JSON
const res = await fetch('/api/kb/create', { method: 'POST', body: JSON.stringify(data) })
const result = await res.json()  // 错误响应是纯文本,会导致解析失败

// ❌ 400 错误使用 error 级别
if (res.status === 400) {
  showMessage('error', await res.text())  // 应该用 info
}

// ❌ 不区分错误类型,统一用 error
if (!res.ok) {
  showMessage('error', '操作失败')  // 应该区分 400 和 500
}

// ❌ 使用 console.error 代替用户提示
if (!res.ok) {
  console.error('请求失败:', await res.text())  // 用户看不到
}

// ❌ 错误响应用 json() 解析
if (!res.ok) {
  const errorData = await res.json()  // 错误响应是纯文本,会解析失败
  showMessage('error', errorData.message)
}
```

**错误提示级别规范**:
- `message.info`: 业务异常、用户输入错误 (HTTP 400-499)
- `message.success`: 操作成功
- `message.warning`: 需要用户注意但不阻塞操作的情况
- `message.error`: 系统错误、网络错误 (HTTP 500+)

**响应解析规则**:
- `res.ok === true` (状态码 200-299): 使用 `res.json()` 解析
- `res.ok === false` (状态码非 200): 使用 `res.text()` 解析

const handleDelete = async (id: number) => {
  try {
    const res = await fetch(`/api/kb/delete?kbName=${kbName}`, {
      method: 'DELETE'
    })
    
    if (!res.ok) {
      const errorText = await res.text()
      if (res.status === 400) {
        msg.info(errorText)  // 业务异常用 info
      } else {
        msg.error('删除失败')  // 系统错误用 error
      }
      return
    }
    
    msg.success('删除成功')
  } catch (err) {
    msg.error('网络请求失败')
  }
}
```

**禁止的做法**:
```typescript
// ❌ 不处理 HTTP 状态码,直接解析 JSON
const res = await fetch('/api/kb/create', { method: 'POST', body: JSON.stringify(data) })
const result = await res.json()
if (result.success) {
  showMessage('success', '成功')
} else {
  showMessage('error', result.error?.message || '失败')  // 400 错误不应该用 error
}

// ❌ 400 错误使用 error 级别
if (res.status === 400) {
  showMessage('error', '参数错误')  // 应该用 info
}

// ❌ 不区分错误类型,统一用 error
if (!res.ok) {
  showMessage('error', '操作失败')  // 应该区分 400 和 500
}

// ❌ 使用 console.error 代替用户提示
if (!res.ok) {
  console.error('请求失败:', await res.text())  // 用户看不到
}
```

**错误提示级别规范**:
- `message.info`: 业务异常、用户输入错误 (HTTP 400)
- `message.success`: 操作成功
- `message.warning`: 需要用户注意但不阻塞操作的情况
- `message.error`: 系统错误、网络错误 (HTTP 500/503)

**响应格式处理**:
- 400 错误: 后端返回纯文本格式 (`text/plain; charset=utf-8`)
- 成功响应: 后端返回 JSON 格式 (`application/json`)
- 前端需要根据 `res.ok` 和 `res.status` 判断,使用 `res.text()` 或 `res.json()` 解析

---

### 5. 禁止通过 psql 直连数据库

**要求**: 所有数据库操作必须通过 Prisma。

**允许的做法**:
- 使用 Prisma Client 方法
- 使用 `prisma.$executeRaw` 执行原生 SQL
- 使用 `prisma.$queryRaw` 查询原生 SQL
- 使用 `prisma migrate dev` 创建迁移
- `prisma/migrations/` 目录下的 `.sql` 文件（由 Prisma 生成和执行）

**允许的示例**:
```typescript
await this.prisma.$executeRaw`CREATE INDEX...`
await prisma.setting.create({ data: {...} })
await prisma.$queryRaw`SELECT * FROM ...`
```

**禁止的做法**:
```typescript
await exec(`psql $DATABASE_URL -c "CREATE INDEX..."`)
await exec(`psql $DATABASE_URL -f seed.sql`)
psql $DATABASE_URL -c "CREATE EXTENSION vector;"
```

---

### 6. 导入路径必须用 `@` 别名

**要求**: 导入路径必须用 `@/` 开头的绝对路径。

**允许的做法**:
- 使用 `@/` 开头的绝对路径
- 同目录文件使用 `./` 相对路径（建议统一用 `@/`）

**允许的示例**:
```typescript
import { resolve } from '@/src/lib/container'
import { ProviderService } from '@/src/services/provider.service'
import { helper } from './helper'  // 同目录允许
```

**禁止的做法**:
```typescript
import { resolve } from '../../src/lib/container'
import { ProviderService } from '../services/provider.service'
import { helper } from './../helper'
```

---

## 数据库迁移流程

**允许的流程**:
1. 修改 `prisma/schema.prisma`
2. 通知用户执行 `pnpm prisma migrate dev`
3. 用户确认迁移成功后继续

**开发环境数据丢失**:
- 迁移时若遇到数据会丢失的情况，直接执行迁移，开发环境数据丢失无所谓
- Prisma 会提示数据丢失风险，输入 `y` 确认即可继续

**禁止的做法**:
- AI 自行执行 `pnpm prisma migrate dev` 或 `pnpm prisma migrate deploy`
- 使用 `psql` 命令直接执行任何 SQL 语句或脚本文件
- 手动修改 `prisma/migrations/` 目录下的 SQL 文件（除非修复迁移错误）
- 因担心数据丢失而停止迁移（开发环境）

---

## 开发服务器

**禁止的做法**:
- AI 自行执行 `pnpm dev`、`npm run dev` 或任何启动开发服务器的命令
- AI 自行执行 `pnpm build`、`npm run build` 或任何构建命令

**原因**: 开发服务器和构建过程需要用户在终端中观察输出和调试。

---

## 遇到无法完成的任务

当遇到以下情况时，立即停止并通知用户：
- 规范禁止的操作无法避免
- 技术限制导致无法实现需求
- 需要修改规范才能完成任务

**通知格式**:
```
任务无法完成，原因：[具体原因]
建议解决方案：[可选的解决方案]
是否继续：需要用户确认
```
