# 开发规范

## 依赖注入规范

### Awilix 容器使用规范

本项目使用 Awilix 作为依赖注入容器，采用 `InjectionMode.PROXY` 模式。

#### 构造函数规范（重要）

**必须使用解构赋值方式声明依赖项，禁止使用 TypeScript 构造函数简写语法。**

##### ✅ 正确写法

```typescript
export class MyService {
  private prisma: PrismaClient
  private otherService: OtherService

  constructor({ prisma, otherService }: { prisma: PrismaClient; otherService: OtherService }) {
    this.prisma = prisma
    this.otherService = otherService
  }
}
```

##### ❌ 错误写法

```typescript
// 禁止使用此写法！
export class MyService {
  constructor(private prisma: PrismaClient, private otherService: OtherService) {}
}
```

#### 原因说明

在 Awilix 的 PROXY 注入模式下，使用 TypeScript 构造函数简写语法会导致以下问题：

1. 当访问 `this.prisma.provider` 时，Awilix 的代理会误将 `provider` 当作需要从容器中解析的依赖项名称
2. 导致运行时错误：`AwilixResolutionError: Could not resolve 'provider'`
3. 这是因为 PROXY 模式会拦截所有属性访问，尝试解析未知的属性名

#### 容器注册规范

在 `src/lib/container.ts` 中注册服务时：

```typescript
// 单例服务（整个应用共享一个实例）
container.register({
  prisma: asValue(prisma as PrismaClient),
  embeddingService: asClass(EmbeddingService).singleton(),
})

// 瞬态服务（每次解析都创建新实例）
container.register({
  chunkService: asClass(ChunkService).transient(),
  kbService: asClass(KBService).transient(),
  providerService: asClass(ProviderService).transient(),
  modelService: asClass(ModelService).transient(),
})
```

#### 服务使用规范

在 API 路由或其他地方使用服务时：

```typescript
import { resolve } from '@/src/lib/container'
import { ProviderService } from '@/src/services/provider.service'

// 正确：使用完整的服务名称
const providerService = resolve<ProviderService>('providerService')

// 错误：不要使用模型名称
// const providerService = resolve('provider') // ❌ 错误！
```

### 已知问题案例

#### 问题：ProviderService 和 ModelService

**症状：**
```
AwilixResolutionError: Could not resolve 'provider'.
Resolution path: provider
```

**原因：**
使用了 `constructor(private prisma: PrismaClient)` 简写语法，当访问 `this.prisma.provider.findMany()` 时，Awilix 代理误解析了 `provider` 属性。

**解决方案：**
改为解构赋值方式：
```typescript
private prisma: PrismaClient

constructor({ prisma }: { prisma: PrismaClient }) {
  this.prisma = prisma
}
```

## 代码风格规范

### TypeScript 类型定义

- 优先使用接口（interface）定义数据结构
- 使用类型别名（type）定义联合类型、交叉类型等
- 避免使用 `any`，优先使用 `unknown` 或具体类型

### 错误处理（重要）

#### 统一异常处理原则

**禁止在服务层使用 try-catch，所有异常处理必须通过统一异常处理中间件完成。**

##### ✅ 正确做法

1. **服务层**：直接抛出异常，不捕获
```typescript
export class MyService {
  async someOperation() {
    // 直接执行操作，让异常自然抛出
    const result = await this.prisma.someModel.findMany()
    return result
  }
}
```

2. **API 层**：使用 `withErrorHandler` 中间件统一处理
```typescript
// pages/api/example.ts
import { withMiddleware } from '@/src/lib/api-utils'

export default withMiddleware(
  async (req, res) => {
    const service = resolve<MyService>('myService')
    const result = await service.someOperation()
    successResponse(res, result)
  },
  { methods: ['GET'] }
)
```

3. **统一异常处理中间件**：捕获所有异常并返回详细错误信息
```typescript
// src/lib/api-utils.ts
export function withErrorHandler(handler) {
  return async (req, res) => {
    try {
      await handler(req, res)
    } catch (error) {
      console.error('API Error:', error)
      
      // 输出完整的错误详情（包括堆栈信息）
      if (error instanceof Error) {
        console.error('Error stack:', error.stack)
      }
      
      // 返回给客户端的错误信息
      let appError: AppError
      if (error instanceof AppError) {
        appError = error
      } else {
        appError = Errors.internalError(
          error instanceof Error 
            ? `${error.message}\nStack: ${error.stack}` 
            : 'Unknown error'
        )
      }
      
      res.status(appError.statusCode).json(appError.toJSON())
    }
  }
}
```

##### ❌ 错误做法

```typescript
// ❌ 服务层禁止使用 try-catch
export class MyService {
  async someOperation() {
    try {
      const result = await this.prisma.someModel.findMany()
      return result
    } catch (error) {
      // 这是错误的！服务层不应该捕获异常
      throw Errors.databaseError(error.message)
    }
  }
}
```

#### 异常类型

使用 `Errors` 工厂函数创建标准异常：

```typescript
// 参数验证错误
throw Errors.badRequest('参数不能为空')

// 资源不存在错误
throw Errors.memoryNotFound(id)

// 数据库错误
throw Errors.databaseError('连接失败')

// 外部服务错误
throw Errors.embeddingError('Ollama 服务不可用')
```

#### 前端错误处理

前端代码（React 组件）可以使用 try-catch 处理网络请求错误，以提供友好的用户提示：

```typescript
// ✅ 前端可以使用 try-catch
const loadData = async () => {
  try {
    const res = await fetch('/api/data')
    const data = await res.json()
    setData(data)
  } catch (error) {
    // 向用户显示友好的错误提示
    showMessage('error', '加载数据失败，请稍后重试')
  }
}
```

#### 特殊情况：降级处理

服务层可以使用 try-catch 进行降级处理，但必须满足以下条件：

1. 有明确的降级方案（fallback）
2. 降级后系统仍能正常工作
3. 记录了降级原因的日志

```typescript
// ✅ 允许：有明确降级方案的 try-catch
async chunkText(text: string) {
  let cutPoints: CutPoint[]
  try {
    // 尝试使用 AI 模型切割
    cutPoints = await this.cutModelService.analyzeCutPoints(text)
  } catch (error) {
    // 降级为规则切割（系统仍能正常工作）
    console.warn('Model-based chunking failed, falling back to rule-based:', error)
    cutPoints = this.ruleBasedChunk(text)
  }
  // 继续处理...
}
```

#### 错误日志规范

- 所有异常必须在统一异常处理中间件中记录完整日志
- 日志必须包含：错误消息、错误堆栈、请求路径、请求参数
- 生产环境应将错误日志写入文件或日志服务

### 数据库操作

- 使用 Prisma Client 进行数据库操作
- 确保在修改 schema 后运行 `pnpm prisma generate`
- 使用事务处理复杂的数据库操作

## 文件组织规范

### 目录结构

```
src/
├── lib/           # 工具库和配置
│   ├── container.ts
│   ├── prisma.ts
│   └── errors.ts
├── services/      # 业务服务层
│   ├── provider.service.ts
│   ├── model.service.ts
│   └── ...
└── types/         # 类型定义
    └── index.ts
```

### 命名规范

- 服务文件：`*.service.ts`
- API 路由：`pages/api/*/index.ts`
- 类型定义：`src/types/index.ts`
- 常量定义：`src/lib/constants.ts` 或相关模块内
