# 开发规范

## 强制规则

### 1. 依赖注入必须用解构赋值

**要求**: 构造函数必须用解构赋值,禁止 TypeScript 简写语法。

**原因**: Awilix PROXY 模式会拦截属性访问,简写语法会导致 `this.prisma.provider` 被误解析为依赖项。

```typescript
// ✅ 正确
constructor({ prisma }: { prisma: PrismaClient }) {
  this.prisma = prisma
}

// ❌ 错误
constructor(private prisma: PrismaClient) {}
```

### 2. 实现代码禁止注释

**要求**: 方法内部禁止任何注释,只允许在公共方法上用 JSDoc。

**原因**: 注释容易过时且干扰阅读,应该用清晰的命名和结构表达意图。

```typescript
// ✅ 正确
/**
 * 获取用户列表
 */
async listUsers() {
  return this.prisma.user.findMany({ where: { isActive: true } })
}

// ❌ 错误
async listUsers() {
  // 查询活跃用户
  return this.prisma.user.findMany({ where: { isActive: true } })
}
```

### 3. 服务层禁止 try-catch

**要求**: 服务层直接抛异常,由 API 层的 `withErrorHandler` 统一处理。

**原因**: try-catch 会隐藏异常来源和堆栈,统一处理才能保证错误信息完整。

```typescript
// ✅ 正确 - 服务层
async someOperation() {
  return this.prisma.someModel.findMany()  // 让异常自然抛出
}

// ✅ 正确 - API 层
export default withMiddleware(async (req, res) => {
  const result = await service.someOperation()
  successResponse(res, result)
}, { methods: ['GET'] })

// ❌ 错误
async someOperation() {
  try {
    return this.prisma.someModel.findMany()
  } catch (error) {
    throw Errors.databaseError(error.message)  // 丢失堆栈
  }
}
```

### 4. 禁止通过 psql 直连数据库

**要求**: 禁止使用 `psql` 命令直接执行 SQL,所有数据库操作必须通过 Prisma。

**原因**: Prisma 提供类型安全、迁移管理和统一接口,psql 直连会绕过这些保障。

**说明**: `prisma/migrations/` 目录下的 `.sql` 文件是 Prisma 迁移系统的一部分,允许存在。这些文件由 `prisma migrate dev` 自动生成或由 Prisma 执行,不是手动直连。

```typescript
// ✅ 正确 - 通过 Prisma 执行
await this.prisma.$executeRaw`CREATE INDEX...`
await prisma.setting.create({ data: {...} })

// ❌ 错误 - 通过 psql 直连
await exec(`psql $DATABASE_URL -c "CREATE INDEX..."`)
await exec(`psql $DATABASE_URL -f seed.sql`)
psql $DATABASE_URL -c "CREATE EXTENSION vector;"
```

### 5. 导入路径必须用 `@` 别名

**要求**: 禁止相对路径导入,必须用 `@/` 开头的绝对路径。

**原因**: 文件移动时不需要改路径,且路径更清晰。

```typescript
// ✅ 正确
import { resolve } from '@/src/lib/container'
import { ProviderService } from '@/src/services/provider.service'

// ❌ 错误
import { resolve } from '../../src/lib/container'
import { ProviderService } from '../services/provider.service'
```

## 允许的例外

### 注释例外

以下情况必须注释:
- 公共 API 的 JSDoc
- 复杂正则表达式
- 配置文件说明

```typescript
// ✅ 必需
const jsonMatch = jsonStr.match(/```(?:json)?\s*([\s\S]*?)```/)  // 提取 markdown 代码块中的 JSON
```

### try-catch 例外

前端代码可以用 try-catch 处理网络错误,提供用户友好提示。

```typescript
// ✅ 前端允许
const loadData = async () => {
  try {
    const res = await fetch('/api/data')
    setData(await res.json())
  } catch (error) {
    showMessage('error', '加载失败')
  }
}
```

### 导入路径例外

同目录文件可以用相对路径 `./`,但建议统一用 `@/`。

## 数据库迁移流程

1. 修改 `prisma/schema.prisma`
2. 运行 `pnpm prisma migrate dev --name <name>`
3. 检查生成的 SQL 文件
4. 测试后提交 schema 和迁移文件

**禁止**: 使用 `psql` 命令直接执行任何 SQL 语句或脚本文件。
