# 开发规范

## 核心原则

**除明确允许的做法外，其他做法全部禁止。遇到无法完成的任务时，立即停止并通知用户。**

---

## 强制规则

### 1. 依赖注入必须用解构赋值

**要求**: 构造函数必须用解构赋值，禁止 TypeScript 简写语法。

**原因**: Awilix PROXY 模式会拦截属性访问，简写语法会导致 `this.prisma.provider` 被误解析为依赖项。

---

### 2. 依赖注入生命周期必须为请求作用域

**要求**: 所有服务的依赖注入生命周期必须为请求作用域（scoped），禁止使用单例（singleton）或瞬态（transient）。

**原因**: 
- 单例服务会在整个应用生命周期内共享状态，可能导致请求间数据污染
- 瞬态服务在同一个请求内多次解析会创建多个实例，浪费资源且可能导致状态不一致
- 请求作用域（scoped）确保同一请求内共享同一实例，不同请求间相互隔离

**例外**:
- `prisma` 客户端本身可以使用 `asValue` 注册（Prisma 连接池管理是线程安全的）
- 纯无状态的工具类服务（需经团队评审确认）

---

### 3. 实现代码禁止注释

**要求**: 方法内部禁止任何注释。

**例外**:
- 公共方法上的 JSDoc 文档注释
- 复杂正则表达式后的单行说明注释
- 配置文件中的说明注释

---

### 4. 服务层禁止 try-catch

**要求**: 服务层直接抛异常，由 API 层统一处理。

---

### 5. 异常分类与响应规范

**要求**: 用户输入导致的业务异常响应 400，其他所有异常响应 500。

**后端响应格式**:
- 成功响应 (200): JSON 格式
- 错误响应 (非 200): 纯文本格式，内容为错误消息

**前端处理规范**:
- HTTP 400: 使用 `message.info` 提示具体错误内容
- HTTP 500: 使用 `message.error` 提示"系统错误"

---

### 6. 禁止 AI 执行任何数据库查询操作

**要求**: AI 禁止以任何方式查询或操作数据库。

**禁止**:
- 使用 `psql` 命令直接执行 SQL
- 使用 `prisma db execute` 执行 SQL
- 使用 `prisma studio` 查看数据
- 任何形式的数据库查询、插入、更新、删除操作

**说明**: 数据库状态由用户自行管理和确认，AI 只能通过代码逻辑推断或要求用户提供信息。

---

### 7. 导入路径必须用 `@` 别名

**要求**: 导入路径必须用 `@/` 开头的绝对路径，同目录文件可使用 `./` 相对路径，禁止使用 `../` 相对路径。

---

### 8. 统一使用 App Router

**要求**: 项目统一使用 Next.js App Router 架构，禁止使用 Pages Router（API 路由和 Swagger 文档除外）。

---

### 9. Page.tsx 作为转发器

**要求**: `app/**/page.tsx` 文件应该只是一个转发器，具体的业务逻辑和 UI 应该分散到组件中。组件文件统一放在 `src/components/` 目录下，按功能模块组织。

---

### 10. Swagger 页面使用 Pages Router

**要求**: Swagger API 文档页面必须使用 Pages Router 实现。

---

## 数据库迁移流程

**流程**:
1. 修改 `prisma/schema.prisma`
2. 将需要执行的迁移命令提供给用户执行
3. 用户确认迁移成功后继续

**禁止**:
- AI 自行执行 `pnpm prisma migrate dev` 或 `pnpm prisma migrate deploy`
- 使用 `psql` 命令直接执行任何 SQL 语句或脚本文件

---

## 开发服务器

**禁止**: AI 自行执行 `pnpm dev`、`pnpm build` 或任何启动/构建命令。

---

## 依赖管理

**禁止**: AI 自行安装 npm 包或修改 `package.json` 的 dependencies。

**要求**: 遇到缺少依赖包的情况时，立即停止并通知用户需要安装的包名及版本。

---

## 遇到无法完成的任务

当遇到以下情况时，立即停止并通知用户：
- 规范禁止的操作无法避免
- 技术限制导致无法实现需求
- 需要修改规范才能完成任务

**通知格式**:
```
任务无法完成，原因：[具体原因]
建议解决方案：[可选的解决方案]
是否继续：需要用户确认
```
