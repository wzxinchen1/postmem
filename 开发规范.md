# 开发规范

## 核心原则

**除明确允许的做法外，其他做法全部禁止。遇到无法完成的任务时，立即停止并通知用户。**

---

## 强制规则

### 1. 依赖注入必须用解构赋值

**要求**: 构造函数必须用解构赋值，禁止 TypeScript 简写语法。

**原因**: Awilix PROXY 模式会拦截属性访问，简写语法会导致 `this.prisma.provider` 被误解析为依赖项。

**允许**:
```typescript
constructor({ prisma }: { prisma: PrismaClient }) {
  this.prisma = prisma
}
```

**禁止**:
```typescript
constructor(private prisma: PrismaClient) {}
```

---

### 2. 实现代码禁止注释

**要求**: 方法内部禁止任何注释。

**允许**:
- 公共方法上的 JSDoc 文档注释
- 复杂正则表达式后的单行说明注释
- 配置文件中的说明注释

---

### 3. 服务层禁止 try-catch

**要求**: 服务层直接抛异常，由 API 层的 `withErrorHandler` 统一处理。

**允许**:
- 服务层让异常自然抛出
- API 层使用 `withMiddleware` 包装
- 前端代码使用 try-catch 处理网络错误

---

### 4. 异常分类与响应规范

**要求**: 业务异常和用户输入导致的异常统一响应 400 错误。

**异常分类**:
- **业务异常**: 业务逻辑校验失败（如资源不存在、状态不满足条件等）
- **用户输入异常**: 参数校验失败（如必填项缺失、格式错误等）
- **系统异常**: 数据库错误、外部服务不可用等（响应 500/503）

**错误码规范**:
- `BAD_REQUEST` (400): 用户输入异常、一般业务异常
- `KB_NOT_FOUND` (404): 知识库不存在
- `MEMORY_NOT_FOUND` (404): 片段不存在
- `EMBEDDING_ERROR` (503): 嵌入模型服务不可用
- `CUT_MODEL_ERROR` (503): 切割模型服务不可用
- `DATABASE_ERROR` (500): 数据库操作异常
- `INTERNAL_ERROR` (500): 其他内部错误

**后端响应格式**:
- 成功响应 (200): JSON 格式 `{ success: true, data: {...} }`
- 错误响应 (非 200): 纯文本格式，内容为错误消息

**前端处理规范**:
- `res.ok === true`: 使用 `res.json()` 解析
- `res.ok === false`: 使用 `res.text()` 解析
- HTTP 400-499: 使用 `message.info` 提示
- HTTP 500+: 使用 `message.error` 提示

**添加新错误码流程**:
1. 在 `src/lib/errors.ts` 的 `ErrorCode` 枚举中添加
2. 在 `ErrorCodeToStatus` 中映射 HTTP 状态码
3. 在 `Errors` 对象中添加工厂函数
4. 在服务层使用新错误码抛出异常

---

### 5. 禁止通过 psql 直连数据库

**要求**: 所有数据库操作必须通过 Prisma。

**允许**:
- 使用 Prisma Client 方法
- 使用 `prisma.$executeRaw` 执行原生 SQL
- 使用 `prisma.$queryRaw` 查询原生 SQL
- 使用 `prisma migrate dev` 创建迁移

**禁止**:
- 使用 `psql` 命令直接执行 SQL

---

### 6. 导入路径必须用 `@` 别名

**要求**: 导入路径必须用 `@/` 开头的绝对路径。

**允许**:
- 使用 `@/` 开头的绝对路径
- 同目录文件使用 `./` 相对路径

**禁止**:
- 使用 `../` 相对路径

---

### 7. 统一使用 App Router

**要求**: 项目统一使用 Next.js App Router 架构，禁止使用 Pages Router（API 路由和 Swagger 文档除外）。

**允许**:
- 使用 `app/` 目录组织页面和布局
- Pages Router 仅用于 API 路由（`pages/api/`）和 Swagger 文档页面

**禁止**:
- 使用 Pages Router 定义普通页面

---

### 8. Page.tsx 作为转发器

**要求**: `app/**/page.tsx` 文件应该只是一个转发器，具体的业务逻辑和 UI 应该分散到组件中。

**允许**:
- `page.tsx` 只负责导入和渲染主组件
- `page.tsx` 可以传递简单的 props（如路由参数）
- 组件文件统一放在 `src/components/` 目录下，按功能模块组织

**禁止**:
- 在 `page.tsx` 中直接实现复杂逻辑
- 组件文件分散在各个 `app/**/components/` 目录下

**组件组织结构**:
```
src/
├── components/
│   ├── admin/          # 管理后台相关组件
│   ├── kb/             # 知识库相关组件
│   ├── common/         # 通用组件
│   └── layout/         # 布局组件

app/
├── admin/
│   └── page.tsx        # 转发器
└── kb/
    └── page.tsx        # 转发器
```

---

### 9. Swagger 页面使用 Pages Router

**要求**: Swagger API 文档页面必须使用 Pages Router 实现。

**允许**:
- 在 `pages/api-docs.tsx` 中实现 Swagger UI
- 在 `pages/api/swagger.json` 或 `pages/api/swagger.ts` 中生成 Swagger 规范

**禁止**:
- 在 `app/` 目录下实现 Swagger 相关页面

---

## 数据库迁移流程

**流程**:
1. 修改 `prisma/schema.prisma`
2. 通知用户执行 `pnpm prisma migrate dev`
3. 用户确认迁移成功后继续

**开发环境数据丢失**: 直接执行迁移，开发环境数据丢失无所谓

**禁止**:
- AI 自行执行 `pnpm prisma migrate dev` 或 `pnpm prisma migrate deploy`
- 使用 `psql` 命令直接执行任何 SQL 语句或脚本文件

---

## 开发服务器

**禁止**:
- AI 自行执行 `pnpm dev`、`npm run dev` 或任何启动开发服务器的命令
- AI 自行执行 `pnpm build`、`npm run build` 或任何构建命令

---

## 遇到无法完成的任务

当遇到以下情况时，立即停止并通知用户：
- 规范禁止的操作无法避免
- 技术限制导致无法实现需求
- 需要修改规范才能完成任务

**通知格式**:
```
任务无法完成，原因：[具体原因]
建议解决方案：[可选的解决方案]
是否继续：需要用户确认
```
